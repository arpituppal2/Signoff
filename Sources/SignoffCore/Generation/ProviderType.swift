// Compatibility shim for incremental XCBuild file lists that still reference
// this path after the GenerationProvider surface moved into Providers.swift.
// Intentionally empty of declarations (see GenerationProvider /
// GenerationProviderType typealias there).
